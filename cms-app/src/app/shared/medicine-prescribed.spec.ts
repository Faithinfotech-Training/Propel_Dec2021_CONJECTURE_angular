import { MedicinePrescribed } from './medicine-prescribed';

describe('MedicinePrescribed', () => {
  it('should create an instance', () => {
    expect(new MedicinePrescribed()).toBeTruthy();
  });
});
